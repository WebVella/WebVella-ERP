ErpApp.constant(
	"$config", {
		//URL relative or full, to the database API handler
		apiBaseUrl: "/api/",
		//How many records you want to be returned on each list page before paging and also the paging size
		pageSize: 10
	}
);